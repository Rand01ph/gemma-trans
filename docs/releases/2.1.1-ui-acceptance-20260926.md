# 2.1.1 图形验收记录（2026-09-26）

## 结论

最新原生 UI 自动化未通过，正式发布流程保持禁用。产品源为 main `f27d6280c690a8b6fe31542e1fc3a255e3f748ee`。74 张基线图片保持原样，补传时逐张 SHA-256 校验一致。

## 环境与执行

- macOS 27.0，build 26A428，与基线系统记录一致；其余截图环境条件仍须在完整比较时核验。
- 用户明确授权启用 DevToolsSecurity，命令成功后读取状态为 enabled。此后测试能进入实际交互，旧的 enabling automation mode 超时未再出现。
- 执行 `script/build_and_run.sh --ui-test`，使用隔离 UITest 身份；结果保存在本机 `App/build/ui-migration-20260926.xcresult`。
- 5 项测试：2 项通过、2 项失败、1 项跳过。服务测试需要独立真实模型入口条件，本轮未启用，不能算通过。

## 结果

| 检查 | 结果 |
| --- | --- |
| 完成卡片速率、复制、固定、关闭 | 通过 |
| 运行中停止与旧速率清除 | 通过 |
| 主窗口清空后打开设置 | 自动化未找到预期设置窗口，失败 |
| 原生菜单截图 | 未找到具有非空 frame 的菜单容器，失败；一次聚焦重跑曾通过，尚不稳定 |

直接操作原始产品构建可完成清空并打开设置；通用、模型与集成页在辅助检查中可访问。这不替代原生自动化和完整像素比较。诊断日志曾显示设置窗口已创建且获得焦点，但不在 active Space，需要继续定位该环境下的窗口/自动化交互问题。

试验性的窗口策略及测试定位改动均已撤回。产品代码与 main 保持一致；没有跳过断言或重新生成基线来获得通过结果。

## 下一步

稳定复现并解决设置窗口定位及菜单显示问题，再跑完整隔离 UI 测试、32 场景截图和像素/元素比较。完成后才能更新此门槛并启用正式归档。
