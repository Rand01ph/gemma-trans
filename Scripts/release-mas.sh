#!/bin/zsh
# Retained to make old automation fail clearly rather than upload a free-only App Store build.
set -euo pipefail
print -u2 'GemmaTrans 2.2 App Store publishing has moved to the private distribution repository.'
print -u2 'For a public sandbox build, generate App/project.yml and build the GemmaTrans-MAS scheme without uploading.'
exit 1
