name: App Store publishing moved
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  distribution-notice:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "::error::GemmaTrans 2.2 App Store builds must use the private distribution repository. This repository publishes the free GitHub build."
          exit 1
